// Phase 6 Task 6.2 will replace this stub with the real mock-proof scanner.
// Until then, exit 0 so `npm run build` still works in development.
console.log('[prelaunch-mock-guard] stub — full implementation in Phase 6 Task 6.2');
process.exit(0);
